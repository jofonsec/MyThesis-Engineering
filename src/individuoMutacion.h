#include <eoSwapMutation.h>
#include <eoShiftMutation.h>
#include <Individuo.h>
// Se genera un objeto (individuoMutacion) de la clase eoSwapMutation, y el template se define para el cromosoma QUEEN el cual fue definido en el archivo queen.h como un eoInit<Minimizing..> lo que lo define como un eoVector.
typedef eoSwapMutation<Individuo> individuoMutacion0;
